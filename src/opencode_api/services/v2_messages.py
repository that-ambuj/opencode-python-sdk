# This file was generated by liblab | https://liblab.com/

from typing import Union
from .utils.validator import Validator
from .utils.base_service import BaseService
from ..net.transport.serializer import Serializer
from ..net.transport.opencode_response import OpencodeResponse, OpencodeMetadata
from ..net.environment.environment import Environment
from ..models.utils.sentinel import SENTINEL
from ..models.utils.cast_models import cast_models
from ..models import BadRequestError, V2SessionMessagesOrder, V2SessionMessagesResponse


class V2MessagesService(BaseService):
    """
    Service class for V2MessagesService operations.
    Provides methods to interact with V2MessagesService-related API endpoints.
    Inherits common functionality from BaseService including authentication and request handling.
    """

    @cast_models
    def v2_session_messages(
        self,
        session_id: str,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
        limit: float = SENTINEL,
        order: V2SessionMessagesOrder = SENTINEL,
        cursor: str = SENTINEL,
    ) -> OpencodeResponse[V2SessionMessagesResponse]:
        """Retrieve projected v2 messages for a session. Items keep the requested order across pages; use cursor.next or cursor.previous to move through the ordered timeline.

        :param session_id: session_id
        :type session_id: str
        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        :param limit: limit, defaults to None
        :type limit: float, optional
        :param order: order, defaults to None
        :type order: V2SessionMessagesOrder, optional
        :param cursor: cursor, defaults to None
        :type cursor: str, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: V2SessionMessagesResponse
        """

        Validator(str).pattern("^ses").validate(session_id)
        Validator(str).is_optional().validate(directory)
        Validator(str).is_optional().validate(workspace)
        Validator(float).is_optional().validate(limit)
        Validator(V2SessionMessagesOrder).is_optional().validate(order)
        Validator(str).is_optional().validate(cursor)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/api/session/{{sessionID}}/message",
                [self.get_access_token()],
            )
            .add_path("sessionID", session_id)
            .add_query("directory", directory)
            .add_query("workspace", workspace)
            .add_query("limit", limit)
            .add_query("order", order)
            .add_query("cursor", cursor)
            .add_error(400, BadRequestError)
            .serialize()
            .set_method("GET")
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=V2SessionMessagesResponse._unmap(response),
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )
