# This file was generated by liblab | https://liblab.com/

from typing import List, Union
from .utils.validator import Validator
from .utils.base_service import BaseService
from ..net.transport.serializer import Serializer
from ..net.transport.opencode_response import OpencodeResponse, OpencodeMetadata
from ..net.environment.environment import Environment
from ..models.utils.sentinel import SENTINEL
from ..models.utils.cast_models import cast_models
from ..models import (
    Archived,
    BadRequestError,
    ConsoleState,
    EffectHttpApiErrorInternalServerError,
    ExperimentalConsoleListOrgsOkResponse,
    ExperimentalConsoleSwitchOrgRequest,
    ExperimentalSessionListRoots,
    GlobalSession,
    ToolListItem,
    Worktree,
    WorktreeCreateInput,
    WorktreeError,
    WorktreeRemoveInput,
    WorktreeResetInput,
)


class ExperimentalService(BaseService):
    """
    Service class for ExperimentalService operations.
    Provides methods to interact with ExperimentalService-related API endpoints.
    Inherits common functionality from BaseService including authentication and request handling.
    """

    @cast_models
    def experimental_console_get(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> OpencodeResponse[ConsoleState]:
        """Get the active Console org name and the set of provider IDs managed by that Console org.

        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: ConsoleState
        """

        Validator(str).is_optional().validate(directory)
        Validator(str).is_optional().validate(workspace)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/experimental/console",
                [self.get_access_token()],
            )
            .add_query("directory", directory)
            .add_query("workspace", workspace)
            .add_error(500, EffectHttpApiErrorInternalServerError)
            .serialize()
            .set_method("GET")
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=ConsoleState._unmap(response),
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )

    @cast_models
    def experimental_console_list_orgs(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> OpencodeResponse[ExperimentalConsoleListOrgsOkResponse]:
        """Get the available Console orgs across logged-in accounts, including the current active org.

        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: ExperimentalConsoleListOrgsOkResponse
        """

        Validator(str).is_optional().validate(directory)
        Validator(str).is_optional().validate(workspace)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/experimental/console/orgs",
                [self.get_access_token()],
            )
            .add_query("directory", directory)
            .add_query("workspace", workspace)
            .add_error(500, EffectHttpApiErrorInternalServerError)
            .serialize()
            .set_method("GET")
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=ExperimentalConsoleListOrgsOkResponse._unmap(response),
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )

    @cast_models
    def experimental_console_switch_org(
        self,
        request_body: ExperimentalConsoleSwitchOrgRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> OpencodeResponse[bool]:
        """Persist a new active Console account/org selection for the current local OpenCode state.

        :param request_body: The request body., defaults to None
        :type request_body: ExperimentalConsoleSwitchOrgRequest, optional
        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: bool
        """

        Validator(ExperimentalConsoleSwitchOrgRequest).is_optional().validate(
            request_body
        )
        Validator(str).is_optional().validate(directory)
        Validator(str).is_optional().validate(workspace)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/experimental/console/switch",
                [self.get_access_token()],
            )
            .add_query("directory", directory)
            .add_query("workspace", workspace)
            .serialize()
            .set_method("POST")
            .set_body(request_body)
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=response,
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )

    @cast_models
    def tool_list(
        self,
        provider: str,
        model: str,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> OpencodeResponse[List[ToolListItem]]:
        """Get a list of available tools with their JSON schema parameters for a specific provider and model combination.

        :param provider: provider
        :type provider: str
        :param model: model
        :type model: str
        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: List[ToolListItem]
        """

        Validator(str).validate(provider)
        Validator(str).validate(model)
        Validator(str).is_optional().validate(directory)
        Validator(str).is_optional().validate(workspace)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/experimental/tool",
                [self.get_access_token()],
            )
            .add_query("directory", directory)
            .add_query("workspace", workspace)
            .add_query("provider", provider)
            .add_query("model", model)
            .add_error(400, BadRequestError)
            .serialize()
            .set_method("GET")
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=[ToolListItem._unmap(item) for item in response],
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )

    @cast_models
    def tool_ids(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> OpencodeResponse[List[str]]:
        """Get a list of all available tool IDs, including both built-in tools and dynamically registered tools.

        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: List[str]
        """

        Validator(str).is_optional().validate(directory)
        Validator(str).is_optional().validate(workspace)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/experimental/tool/ids",
                [self.get_access_token()],
            )
            .add_query("directory", directory)
            .add_query("workspace", workspace)
            .add_error(400, BadRequestError)
            .serialize()
            .set_method("GET")
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=response,
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )

    @cast_models
    def worktree_list(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> OpencodeResponse[List[str]]:
        """List all sandbox worktrees for the current project.

        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: List[str]
        """

        Validator(str).is_optional().validate(directory)
        Validator(str).is_optional().validate(workspace)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/experimental/worktree",
                [self.get_access_token()],
            )
            .add_query("directory", directory)
            .add_query("workspace", workspace)
            .add_error(400, WorktreeError)
            .serialize()
            .set_method("GET")
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=response,
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )

    @cast_models
    def worktree_create(
        self,
        request_body: WorktreeCreateInput = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> OpencodeResponse[Worktree]:
        """Create a new git worktree for the current project and run any configured startup scripts.

        :param request_body: The request body., defaults to None
        :type request_body: WorktreeCreateInput, optional
        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: Worktree
        """

        Validator(WorktreeCreateInput).is_optional().validate(request_body)
        Validator(str).is_optional().validate(directory)
        Validator(str).is_optional().validate(workspace)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/experimental/worktree",
                [self.get_access_token()],
            )
            .add_query("directory", directory)
            .add_query("workspace", workspace)
            .add_error(400, WorktreeError)
            .serialize()
            .set_method("POST")
            .set_body(request_body)
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=Worktree._unmap(response),
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )

    @cast_models
    def worktree_remove(
        self,
        request_body: WorktreeRemoveInput = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> OpencodeResponse[bool]:
        """Remove a git worktree and delete its branch.

        :param request_body: The request body., defaults to None
        :type request_body: WorktreeRemoveInput, optional
        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: bool
        """

        Validator(WorktreeRemoveInput).is_optional().validate(request_body)
        Validator(str).is_optional().validate(directory)
        Validator(str).is_optional().validate(workspace)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/experimental/worktree",
                [self.get_access_token()],
            )
            .add_query("directory", directory)
            .add_query("workspace", workspace)
            .add_error(400, WorktreeError)
            .serialize()
            .set_method("DELETE")
            .set_body(request_body)
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=response,
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )

    @cast_models
    def worktree_reset(
        self,
        request_body: WorktreeResetInput = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> OpencodeResponse[bool]:
        """Reset a worktree branch to the primary default branch.

        :param request_body: The request body., defaults to None
        :type request_body: WorktreeResetInput, optional
        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: bool
        """

        Validator(WorktreeResetInput).is_optional().validate(request_body)
        Validator(str).is_optional().validate(directory)
        Validator(str).is_optional().validate(workspace)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/experimental/worktree/reset",
                [self.get_access_token()],
            )
            .add_query("directory", directory)
            .add_query("workspace", workspace)
            .add_error(400, WorktreeError)
            .serialize()
            .set_method("POST")
            .set_body(request_body)
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=response,
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )

    @cast_models
    def experimental_session_list(
        self,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
        roots: ExperimentalSessionListRoots = SENTINEL,
        start: float = SENTINEL,
        cursor: float = SENTINEL,
        search: str = SENTINEL,
        limit: float = SENTINEL,
        archived: Archived = SENTINEL,
    ) -> OpencodeResponse[List[GlobalSession]]:
        """Get a list of all OpenCode sessions across projects, sorted by most recently updated. Archived sessions are excluded by default.

        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        :param roots: roots, defaults to None
        :type roots: ExperimentalSessionListRoots, optional
        :param start: start, defaults to None
        :type start: float, optional
        :param cursor: cursor, defaults to None
        :type cursor: float, optional
        :param search: search, defaults to None
        :type search: str, optional
        :param limit: limit, defaults to None
        :type limit: float, optional
        :param archived: archived, defaults to None
        :type archived: Archived, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: List[GlobalSession]
        """

        Validator(str).is_optional().validate(directory)
        Validator(str).is_optional().validate(workspace)
        Validator(ExperimentalSessionListRoots).is_optional().validate(roots)
        Validator(float).is_optional().validate(start)
        Validator(float).is_optional().validate(cursor)
        Validator(str).is_optional().validate(search)
        Validator(float).is_optional().validate(limit)
        Validator(Archived).is_optional().validate(archived)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/experimental/session",
                [self.get_access_token()],
            )
            .add_query("directory", directory)
            .add_query("workspace", workspace)
            .add_query("roots", roots)
            .add_query("start", start)
            .add_query("cursor", cursor)
            .add_query("search", search)
            .add_query("limit", limit)
            .add_query("archived", archived)
            .serialize()
            .set_method("GET")
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=[GlobalSession._unmap(item) for item in response],
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )

    @cast_models
    def experimental_resource_list(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> OpencodeResponse[dict]:
        """Get all available MCP resources from connected servers. Optionally filter by name.

        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: dict
        """

        Validator(str).is_optional().validate(directory)
        Validator(str).is_optional().validate(workspace)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/experimental/resource",
                [self.get_access_token()],
            )
            .add_query("directory", directory)
            .add_query("workspace", workspace)
            .serialize()
            .set_method("GET")
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=response,
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )
