# This file was generated by liblab | https://liblab.com/

from typing import Awaitable, Union
from .utils.to_async import to_async
from ..mcp import McpService
from ...models.utils.sentinel import SENTINEL
from ...models import (
    McpAddRequest,
    McpAuthStartOkResponse,
    McpAuthRemoveOkResponse,
    McpStatus,
    McpAuthCallbackRequest,
)


class McpServiceAsync(McpService):
    """
    Async Wrapper for McpServiceAsync
    """

    def mcp_status(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[dict]:
        return to_async(super().mcp_status)(directory, workspace)

    def mcp_add(
        self,
        request_body: McpAddRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[dict]:
        return to_async(super().mcp_add)(request_body, directory, workspace)

    def mcp_auth_start(
        self, name: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[McpAuthStartOkResponse]:
        return to_async(super().mcp_auth_start)(name, directory, workspace)

    def mcp_auth_remove(
        self, name: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[McpAuthRemoveOkResponse]:
        return to_async(super().mcp_auth_remove)(name, directory, workspace)

    def mcp_auth_callback(
        self,
        name: str,
        request_body: McpAuthCallbackRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[McpStatus]:
        return to_async(super().mcp_auth_callback)(
            name, request_body, directory, workspace
        )

    def mcp_auth_authenticate(
        self, name: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[McpStatus]:
        return to_async(super().mcp_auth_authenticate)(name, directory, workspace)

    def mcp_connect(
        self, name: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[bool]:
        return to_async(super().mcp_connect)(name, directory, workspace)

    def mcp_disconnect(
        self, name: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[bool]:
        return to_async(super().mcp_disconnect)(name, directory, workspace)
