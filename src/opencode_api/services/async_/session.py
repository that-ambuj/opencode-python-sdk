# This file was generated by liblab | https://liblab.com/

from typing import Awaitable, List, Union
from .utils.to_async import to_async
from ..session import SessionService
from ...models.utils.sentinel import SENTINEL
from ...models import (
    Session,
    Scope,
    SessionListRoots,
    SessionCreateRequest,
    SessionUpdateRequest,
    Todo,
    SnapshotFileDiff,
    SessionMessagesOkResponse,
    SessionPromptOkResponse,
    SessionPromptRequest,
    SessionMessageOkResponse,
    SessionForkRequest,
    SessionInitRequest,
    SessionSummarizeRequest,
    SessionPromptAsyncRequest,
    SessionCommandOkResponse,
    SessionCommandRequest,
    SessionShellOkResponse,
    SessionShellRequest,
    SessionRevertRequest,
    PermissionRespondRequest,
    Part,
)


class SessionServiceAsync(SessionService):
    """
    Async Wrapper for SessionServiceAsync
    """

    def session_list(
        self,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
        scope: Scope = SENTINEL,
        path: str = SENTINEL,
        roots: SessionListRoots = SENTINEL,
        start: float = SENTINEL,
        search: str = SENTINEL,
        limit: float = SENTINEL,
    ) -> Awaitable[List[Session]]:
        return to_async(super().session_list)(
            directory, workspace, scope, path, roots, start, search, limit
        )

    def session_create(
        self,
        request_body: SessionCreateRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[Session]:
        return to_async(super().session_create)(request_body, directory, workspace)

    def session_status(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[dict]:
        return to_async(super().session_status)(directory, workspace)

    def session_get(
        self, session_id: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[Session]:
        return to_async(super().session_get)(session_id, directory, workspace)

    def session_update(
        self,
        session_id: str,
        request_body: SessionUpdateRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[Session]:
        return to_async(super().session_update)(
            session_id, request_body, directory, workspace
        )

    def session_delete(
        self, session_id: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[bool]:
        return to_async(super().session_delete)(session_id, directory, workspace)

    def session_children(
        self, session_id: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[Session]]:
        return to_async(super().session_children)(session_id, directory, workspace)

    def session_todo(
        self, session_id: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[Todo]]:
        return to_async(super().session_todo)(session_id, directory, workspace)

    def session_diff(
        self,
        session_id: str,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
        message_id: str = SENTINEL,
    ) -> Awaitable[List[SnapshotFileDiff]]:
        return to_async(super().session_diff)(
            session_id, directory, workspace, message_id
        )

    def session_messages(
        self,
        session_id: str,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
        limit: int = SENTINEL,
        before: str = SENTINEL,
    ) -> Awaitable[List[SessionMessagesOkResponse]]:
        return to_async(super().session_messages)(
            session_id, directory, workspace, limit, before
        )

    def session_prompt(
        self,
        session_id: str,
        request_body: SessionPromptRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[SessionPromptOkResponse]:
        return to_async(super().session_prompt)(
            session_id, request_body, directory, workspace
        )

    def session_message(
        self,
        session_id: str,
        message_id: str,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[SessionMessageOkResponse]:
        return to_async(super().session_message)(
            session_id, message_id, directory, workspace
        )

    def session_delete_message(
        self,
        session_id: str,
        message_id: str,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[bool]:
        return to_async(super().session_delete_message)(
            session_id, message_id, directory, workspace
        )

    def session_fork(
        self,
        session_id: str,
        request_body: SessionForkRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[Session]:
        return to_async(super().session_fork)(
            session_id, request_body, directory, workspace
        )

    def session_abort(
        self, session_id: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[bool]:
        return to_async(super().session_abort)(session_id, directory, workspace)

    def session_init(
        self,
        session_id: str,
        request_body: SessionInitRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[bool]:
        return to_async(super().session_init)(
            session_id, request_body, directory, workspace
        )

    def session_share(
        self, session_id: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[Session]:
        return to_async(super().session_share)(session_id, directory, workspace)

    def session_unshare(
        self, session_id: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[Session]:
        return to_async(super().session_unshare)(session_id, directory, workspace)

    def session_summarize(
        self,
        session_id: str,
        request_body: SessionSummarizeRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[bool]:
        return to_async(super().session_summarize)(
            session_id, request_body, directory, workspace
        )

    def session_prompt_async(
        self,
        session_id: str,
        request_body: SessionPromptAsyncRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[None]:
        return to_async(super().session_prompt_async)(
            session_id, request_body, directory, workspace
        )

    def session_command(
        self,
        session_id: str,
        request_body: SessionCommandRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[SessionCommandOkResponse]:
        return to_async(super().session_command)(
            session_id, request_body, directory, workspace
        )

    def session_shell(
        self,
        session_id: str,
        request_body: SessionShellRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[SessionShellOkResponse]:
        return to_async(super().session_shell)(
            session_id, request_body, directory, workspace
        )

    def session_revert(
        self,
        session_id: str,
        request_body: SessionRevertRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[Session]:
        return to_async(super().session_revert)(
            session_id, request_body, directory, workspace
        )

    def session_unrevert(
        self, session_id: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[Session]:
        return to_async(super().session_unrevert)(session_id, directory, workspace)

    def permission_respond(
        self,
        session_id: str,
        permission_id: str,
        request_body: PermissionRespondRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[bool]:
        return to_async(super().permission_respond)(
            session_id, permission_id, request_body, directory, workspace
        )

    def part_update(
        self,
        session_id: str,
        message_id: str,
        part_id: str,
        request_body: Part = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[Part]:
        return to_async(super().part_update)(
            session_id, message_id, part_id, request_body, directory, workspace
        )

    def part_delete(
        self,
        session_id: str,
        message_id: str,
        part_id: str,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[bool]:
        return to_async(super().part_delete)(
            session_id, message_id, part_id, directory, workspace
        )
