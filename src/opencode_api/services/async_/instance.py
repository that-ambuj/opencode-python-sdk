# This file was generated by liblab | https://liblab.com/

from typing import Awaitable, List, Union
from .utils.to_async import to_async
from ..instance import InstanceService
from ...models.utils.sentinel import SENTINEL
from ...models import (
    Path,
    VcsInfo,
    VcsFileStatus,
    VcsFileDiff,
    VcsDiffMode,
    VcsApplyOkResponse,
    VcsApplyRequest,
    Command,
    Agent,
    AppSkillsOkResponse,
    LspStatus,
    FormatterStatus,
)


class InstanceServiceAsync(InstanceService):
    """
    Async Wrapper for InstanceServiceAsync
    """

    def instance_dispose(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[bool]:
        return to_async(super().instance_dispose)(directory, workspace)

    def path_get(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[Path]:
        return to_async(super().path_get)(directory, workspace)

    def vcs_get(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[VcsInfo]:
        return to_async(super().vcs_get)(directory, workspace)

    def vcs_status(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[VcsFileStatus]]:
        return to_async(super().vcs_status)(directory, workspace)

    def vcs_diff(
        self, mode: VcsDiffMode, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[VcsFileDiff]]:
        return to_async(super().vcs_diff)(mode, directory, workspace)

    def vcs_diff_raw(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[str]:
        return to_async(super().vcs_diff_raw)(directory, workspace)

    def vcs_apply(
        self,
        request_body: VcsApplyRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[VcsApplyOkResponse]:
        return to_async(super().vcs_apply)(request_body, directory, workspace)

    def command_list(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[Command]]:
        return to_async(super().command_list)(directory, workspace)

    def app_agents(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[Agent]]:
        return to_async(super().app_agents)(directory, workspace)

    def app_skills(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[AppSkillsOkResponse]]:
        return to_async(super().app_skills)(directory, workspace)

    def lsp_status(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[LspStatus]]:
        return to_async(super().lsp_status)(directory, workspace)

    def formatter_status(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[FormatterStatus]]:
        return to_async(super().formatter_status)(directory, workspace)
