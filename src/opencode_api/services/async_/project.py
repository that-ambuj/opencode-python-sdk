# This file was generated by liblab | https://liblab.com/

from typing import Awaitable, List, Union
from .utils.to_async import to_async
from ..project import ProjectService
from ...models.utils.sentinel import SENTINEL
from ...models import Project, ProjectUpdateRequest


class ProjectServiceAsync(ProjectService):
    """
    Async Wrapper for ProjectServiceAsync
    """

    def project_list(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[Project]]:
        return to_async(super().project_list)(directory, workspace)

    def project_current(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[Project]:
        return to_async(super().project_current)(directory, workspace)

    def project_init_git(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[Project]:
        return to_async(super().project_init_git)(directory, workspace)

    def project_update(
        self,
        project_id: str,
        request_body: ProjectUpdateRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[Project]:
        return to_async(super().project_update)(
            project_id, request_body, directory, workspace
        )
