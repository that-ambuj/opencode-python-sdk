# This file was generated by liblab | https://liblab.com/

from typing import Awaitable, Union, List
from .utils.to_async import to_async
from ..sync import SyncService
from ...models.utils.sentinel import SENTINEL
from ...models import (
    SyncReplayOkResponse,
    SyncReplayRequest,
    SyncStealOkResponse,
    SyncStealRequest,
    SyncHistoryListOkResponse,
)


class SyncServiceAsync(SyncService):
    """
    Async Wrapper for SyncServiceAsync
    """

    def sync_start(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[bool]:
        return to_async(super().sync_start)(directory, workspace)

    def sync_replay(
        self,
        request_body: SyncReplayRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[SyncReplayOkResponse]:
        return to_async(super().sync_replay)(request_body, directory, workspace)

    def sync_steal(
        self,
        request_body: SyncStealRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[SyncStealOkResponse]:
        return to_async(super().sync_steal)(request_body, directory, workspace)

    def sync_history_list(
        self,
        request_body: dict = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[List[SyncHistoryListOkResponse]]:
        return to_async(super().sync_history_list)(request_body, directory, workspace)
