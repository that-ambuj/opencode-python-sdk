# This file was generated by liblab | https://liblab.com/

from typing import Awaitable, List, Union
from .utils.to_async import to_async
from ..pty import PtyService
from ...models.utils.sentinel import SENTINEL
from ...models import (
    PtyShellsOkResponse,
    Pty,
    PtyCreateRequest,
    PtyUpdateRequest,
    PtyConnectTokenOkResponse,
)


class PtyServiceAsync(PtyService):
    """
    Async Wrapper for PtyServiceAsync
    """

    def pty_shells(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[PtyShellsOkResponse]]:
        return to_async(super().pty_shells)(directory, workspace)

    def pty_list(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[Pty]]:
        return to_async(super().pty_list)(directory, workspace)

    def pty_create(
        self,
        request_body: PtyCreateRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[Pty]:
        return to_async(super().pty_create)(request_body, directory, workspace)

    def pty_get(
        self, pty_id: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[Pty]:
        return to_async(super().pty_get)(pty_id, directory, workspace)

    def pty_update(
        self,
        pty_id: str,
        request_body: PtyUpdateRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[Pty]:
        return to_async(super().pty_update)(pty_id, request_body, directory, workspace)

    def pty_remove(
        self, pty_id: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[bool]:
        return to_async(super().pty_remove)(pty_id, directory, workspace)

    def pty_connect_token(
        self, pty_id: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[PtyConnectTokenOkResponse]:
        return to_async(super().pty_connect_token)(pty_id, directory, workspace)

    def pty_connect(
        self, pty_id: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[bool]:
        return to_async(super().pty_connect)(pty_id, directory, workspace)
