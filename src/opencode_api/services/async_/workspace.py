# This file was generated by liblab | https://liblab.com/

from typing import Awaitable, List, Union
from .utils.to_async import to_async
from ..workspace import WorkspaceService
from ...models.utils.sentinel import SENTINEL
from ...models import (
    ExperimentalWorkspaceAdapterListOkResponse,
    Workspace,
    ExperimentalWorkspaceCreateRequest,
    ExperimentalWorkspaceStatusOkResponse,
    ExperimentalWorkspaceWarpRequest,
)


class WorkspaceServiceAsync(WorkspaceService):
    """
    Async Wrapper for WorkspaceServiceAsync
    """

    def experimental_workspace_adapter_list(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[ExperimentalWorkspaceAdapterListOkResponse]]:
        return to_async(super().experimental_workspace_adapter_list)(
            directory, workspace
        )

    def experimental_workspace_list(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[Workspace]]:
        return to_async(super().experimental_workspace_list)(directory, workspace)

    def experimental_workspace_create(
        self,
        request_body: ExperimentalWorkspaceCreateRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[Workspace]:
        return to_async(super().experimental_workspace_create)(
            request_body, directory, workspace
        )

    def experimental_workspace_sync_list(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[None]:
        return to_async(super().experimental_workspace_sync_list)(directory, workspace)

    def experimental_workspace_status(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[ExperimentalWorkspaceStatusOkResponse]]:
        return to_async(super().experimental_workspace_status)(directory, workspace)

    def experimental_workspace_remove(
        self, id_: str, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[Workspace]:
        return to_async(super().experimental_workspace_remove)(
            id_, directory, workspace
        )

    def experimental_workspace_warp(
        self,
        request_body: ExperimentalWorkspaceWarpRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[None]:
        return to_async(super().experimental_workspace_warp)(
            request_body, directory, workspace
        )
