# This file was generated by liblab | https://liblab.com/

from typing import Awaitable, Union
from .utils.to_async import to_async
from ..provider import ProviderService
from ...models.utils.sentinel import SENTINEL
from ...models import (
    ProviderListOkResponse,
    ProviderAuthAuthorization,
    ProviderOauthAuthorizeRequest,
    ProviderOauthCallbackRequest,
)


class ProviderServiceAsync(ProviderService):
    """
    Async Wrapper for ProviderServiceAsync
    """

    def provider_list(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[ProviderListOkResponse]:
        return to_async(super().provider_list)(directory, workspace)

    def provider_auth(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[dict]:
        return to_async(super().provider_auth)(directory, workspace)

    def provider_oauth_authorize(
        self,
        provider_id: str,
        request_body: ProviderOauthAuthorizeRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[ProviderAuthAuthorization]:
        return to_async(super().provider_oauth_authorize)(
            provider_id, request_body, directory, workspace
        )

    def provider_oauth_callback(
        self,
        provider_id: str,
        request_body: ProviderOauthCallbackRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[bool]:
        return to_async(super().provider_oauth_callback)(
            provider_id, request_body, directory, workspace
        )
