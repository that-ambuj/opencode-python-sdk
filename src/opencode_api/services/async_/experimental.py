# This file was generated by liblab | https://liblab.com/

from typing import Awaitable, Union, List
from .utils.to_async import to_async
from ..experimental import ExperimentalService
from ...models.utils.sentinel import SENTINEL
from ...models import (
    ConsoleState,
    ExperimentalConsoleListOrgsOkResponse,
    ExperimentalConsoleSwitchOrgRequest,
    ToolListItem,
    Worktree,
    WorktreeCreateInput,
    WorktreeRemoveInput,
    WorktreeResetInput,
    GlobalSession,
    ExperimentalSessionListRoots,
    Archived,
)


class ExperimentalServiceAsync(ExperimentalService):
    """
    Async Wrapper for ExperimentalServiceAsync
    """

    def experimental_console_get(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[ConsoleState]:
        return to_async(super().experimental_console_get)(directory, workspace)

    def experimental_console_list_orgs(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[ExperimentalConsoleListOrgsOkResponse]:
        return to_async(super().experimental_console_list_orgs)(directory, workspace)

    def experimental_console_switch_org(
        self,
        request_body: ExperimentalConsoleSwitchOrgRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[bool]:
        return to_async(super().experimental_console_switch_org)(
            request_body, directory, workspace
        )

    def tool_list(
        self,
        provider: str,
        model: str,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[List[ToolListItem]]:
        return to_async(super().tool_list)(provider, model, directory, workspace)

    def tool_ids(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[str]]:
        return to_async(super().tool_ids)(directory, workspace)

    def worktree_list(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[List[str]]:
        return to_async(super().worktree_list)(directory, workspace)

    def worktree_create(
        self,
        request_body: WorktreeCreateInput = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[Worktree]:
        return to_async(super().worktree_create)(request_body, directory, workspace)

    def worktree_remove(
        self,
        request_body: WorktreeRemoveInput = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[bool]:
        return to_async(super().worktree_remove)(request_body, directory, workspace)

    def worktree_reset(
        self,
        request_body: WorktreeResetInput = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[bool]:
        return to_async(super().worktree_reset)(request_body, directory, workspace)

    def experimental_session_list(
        self,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
        roots: ExperimentalSessionListRoots = SENTINEL,
        start: float = SENTINEL,
        cursor: float = SENTINEL,
        search: str = SENTINEL,
        limit: float = SENTINEL,
        archived: Archived = SENTINEL,
    ) -> Awaitable[List[GlobalSession]]:
        return to_async(super().experimental_session_list)(
            directory, workspace, roots, start, cursor, search, limit, archived
        )

    def experimental_resource_list(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> Awaitable[dict]:
        return to_async(super().experimental_resource_list)(directory, workspace)
