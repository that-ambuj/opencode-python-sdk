# This file was generated by liblab | https://liblab.com/

from typing import Awaitable, Union
from .utils.to_async import to_async
from ..control import ControlService
from ...models.utils.sentinel import SENTINEL
from ...models import Auth, AppLogRequest


class ControlServiceAsync(ControlService):
    """
    Async Wrapper for ControlServiceAsync
    """

    def auth_set(self, provider_id: str, request_body: Auth = None) -> Awaitable[bool]:
        return to_async(super().auth_set)(provider_id, request_body)

    def auth_remove(self, provider_id: str) -> Awaitable[bool]:
        return to_async(super().auth_remove)(provider_id)

    def app_log(
        self,
        request_body: AppLogRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> Awaitable[bool]:
        return to_async(super().app_log)(request_body, directory, workspace)
