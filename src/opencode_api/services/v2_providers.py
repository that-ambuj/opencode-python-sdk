# This file was generated by liblab | https://liblab.com/

from typing import List, Union
from .utils.validator import Validator
from .utils.base_service import BaseService
from ..net.transport.serializer import Serializer
from ..net.transport.opencode_response import OpencodeResponse, OpencodeMetadata
from ..net.environment.environment import Environment
from ..models.utils.sentinel import SENTINEL
from ..models.utils.cast_models import cast_models
from ..models import (
    NotFoundError,
    ProviderV2Info,
    V2ProviderGetLocation,
    V2ProviderListLocation,
)


class V2ProvidersService(BaseService):
    """
    Service class for V2ProvidersService operations.
    Provides methods to interact with V2ProvidersService-related API endpoints.
    Inherits common functionality from BaseService including authentication and request handling.
    """

    @cast_models
    def v2_provider_list(
        self, location: V2ProviderListLocation = SENTINEL
    ) -> OpencodeResponse[List[ProviderV2Info]]:
        """Retrieve active v2 AI providers so clients can show provider availability and configuration.

        :param location: location, defaults to None
        :type location: V2ProviderListLocation, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: List[ProviderV2Info]
        """

        Validator(V2ProviderListLocation).is_optional().validate(location)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/api/provider",
                [self.get_access_token()],
            )
            .add_query("location", location, style="deepObject")
            .serialize()
            .set_method("GET")
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=[ProviderV2Info._unmap(item) for item in response],
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )

    @cast_models
    def v2_provider_get(
        self, provider_id: str, location: V2ProviderGetLocation = SENTINEL
    ) -> OpencodeResponse[ProviderV2Info]:
        """Retrieve a single v2 AI provider so clients can inspect its availability and endpoint settings.

        :param provider_id: provider_id
        :type provider_id: str
        :param location: location, defaults to None
        :type location: V2ProviderGetLocation, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: ProviderV2Info
        """

        Validator(str).validate(provider_id)
        Validator(V2ProviderGetLocation).is_optional().validate(location)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/api/provider/{{providerID}}",
                [self.get_access_token()],
            )
            .add_path("providerID", provider_id)
            .add_query("location", location, style="deepObject")
            .add_error(404, NotFoundError)
            .serialize()
            .set_method("GET")
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=ProviderV2Info._unmap(response),
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )
