# This file was generated by liblab | https://liblab.com/

from typing import Union
from .utils.validator import Validator
from .utils.base_service import BaseService
from ..net.transport.serializer import Serializer
from ..net.transport.opencode_response import OpencodeResponse, OpencodeMetadata
from ..net.environment.environment import Environment
from ..models.utils.sentinel import SENTINEL
from ..models.utils.cast_models import cast_models
from ..models import (
    ProviderAuthAuthorization,
    ProviderAuthError1,
    ProviderListOkResponse,
    ProviderOauthAuthorizeRequest,
    ProviderOauthCallbackRequest,
)


class ProviderService(BaseService):
    """
    Service class for ProviderService operations.
    Provides methods to interact with ProviderService-related API endpoints.
    Inherits common functionality from BaseService including authentication and request handling.
    """

    @cast_models
    def provider_list(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> OpencodeResponse[ProviderListOkResponse]:
        """Get a list of all available AI providers, including both available and connected ones.

        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: ProviderListOkResponse
        """

        Validator(str).is_optional().validate(directory)
        Validator(str).is_optional().validate(workspace)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/provider",
                [self.get_access_token()],
            )
            .add_query("directory", directory)
            .add_query("workspace", workspace)
            .serialize()
            .set_method("GET")
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=ProviderListOkResponse._unmap(response),
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )

    @cast_models
    def provider_auth(
        self, directory: str = SENTINEL, workspace: str = SENTINEL
    ) -> OpencodeResponse[dict]:
        """Retrieve available authentication methods for all AI providers.

        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: dict
        """

        Validator(str).is_optional().validate(directory)
        Validator(str).is_optional().validate(workspace)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/provider/auth",
                [self.get_access_token()],
            )
            .add_query("directory", directory)
            .add_query("workspace", workspace)
            .serialize()
            .set_method("GET")
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=response,
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )

    @cast_models
    def provider_oauth_authorize(
        self,
        provider_id: str,
        request_body: ProviderOauthAuthorizeRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> OpencodeResponse[ProviderAuthAuthorization]:
        """Start the OAuth authorization flow for a provider.

        :param request_body: The request body., defaults to None
        :type request_body: ProviderOauthAuthorizeRequest, optional
        :param provider_id: provider_id
        :type provider_id: str
        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: ProviderAuthAuthorization
        """

        Validator(ProviderOauthAuthorizeRequest).is_optional().validate(request_body)
        Validator(str).validate(provider_id)
        Validator(str).is_optional().validate(directory)
        Validator(str).is_optional().validate(workspace)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/provider/{{providerID}}/oauth/authorize",
                [self.get_access_token()],
            )
            .add_path("providerID", provider_id)
            .add_query("directory", directory)
            .add_query("workspace", workspace)
            .add_error(400, ProviderAuthError1)
            .serialize()
            .set_method("POST")
            .set_body(request_body)
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=ProviderAuthAuthorization._unmap(response),
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )

    @cast_models
    def provider_oauth_callback(
        self,
        provider_id: str,
        request_body: ProviderOauthCallbackRequest = None,
        directory: str = SENTINEL,
        workspace: str = SENTINEL,
    ) -> OpencodeResponse[bool]:
        """Handle the OAuth callback from a provider after user authorization.

        :param request_body: The request body., defaults to None
        :type request_body: ProviderOauthCallbackRequest, optional
        :param provider_id: provider_id
        :type provider_id: str
        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: The parsed response data.
        :rtype: bool
        """

        Validator(ProviderOauthCallbackRequest).is_optional().validate(request_body)
        Validator(str).validate(provider_id)
        Validator(str).is_optional().validate(directory)
        Validator(str).is_optional().validate(workspace)

        serialized_request = (
            Serializer(
                f"{self.base_url or Environment.DEFAULT.url}/provider/{{providerID}}/oauth/callback",
                [self.get_access_token()],
            )
            .add_path("providerID", provider_id)
            .add_query("directory", directory)
            .add_query("workspace", workspace)
            .add_error(400, ProviderAuthError1)
            .serialize()
            .set_method("POST")
            .set_body(request_body)
        )

        response, status, _ = self.send_request(serialized_request)
        return OpencodeResponse(
            data=response,
            raw=self._last_response.raw,
            metadata=OpencodeMetadata(
                headers=dict(self._last_response.headers), status_code=status
            ),
        )
