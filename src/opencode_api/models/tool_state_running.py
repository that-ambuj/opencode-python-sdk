# This file was generated by liblab | https://liblab.com/

from enum import Enum
from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.sentinel import SENTINEL


class ToolStateRunningStatus(Enum):
    """An enumeration representing different categories.

    :cvar RUNNING: "running"
    :vartype RUNNING: str
    """

    RUNNING = "running"

    def list():
        """Lists all category values.

        :return: A list of all category values.
        :rtype: list
        """
        return list(
            map(lambda x: x.value, ToolStateRunningStatus._member_map_.values())
        )


@JsonMap({})
class ToolStateRunningTime(BaseModel):
    """ToolStateRunningTime

    :param start: start
    :type start: int
    """

    def __init__(self, start: int, **kwargs):
        """ToolStateRunningTime

        :param start: start
        :type start: int
        """
        self.start = self._define_number("start", start, ge=0)
        self._kwargs = kwargs


@JsonMap({})
class ToolStateRunning(BaseModel):
    """ToolStateRunning

    :param status: status
    :type status: ToolStateRunningStatus
    :param input: input
    :type input: dict
    :param title: title, defaults to None
    :type title: str, optional
    :param metadata: metadata, defaults to None
    :type metadata: dict, optional
    :param time: time
    :type time: ToolStateRunningTime
    """

    def __init__(
        self,
        status: ToolStateRunningStatus,
        input: dict,
        time: ToolStateRunningTime,
        title: str = SENTINEL,
        metadata: dict = SENTINEL,
        **kwargs
    ):
        """ToolStateRunning

        :param status: status
        :type status: ToolStateRunningStatus
        :param input: input
        :type input: dict
        :param title: title, defaults to None
        :type title: str, optional
        :param metadata: metadata, defaults to None
        :type metadata: dict, optional
        :param time: time
        :type time: ToolStateRunningTime
        """
        self.status = self._enum_matching(
            status, ToolStateRunningStatus.list(), "status"
        )
        self.input = input
        if title is not SENTINEL:
            self.title = self._define_str("title", title)
        if metadata is not SENTINEL:
            self.metadata = metadata
        self.time = self._define_object(time, ToolStateRunningTime)
        self._kwargs = kwargs
