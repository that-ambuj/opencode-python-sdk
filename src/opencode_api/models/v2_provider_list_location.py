# This file was generated by liblab | https://liblab.com/

from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.sentinel import SENTINEL


@JsonMap({})
class V2ProviderListLocation(BaseModel):
    """V2ProviderListLocation

    :param directory: directory, defaults to None
    :type directory: str, optional
    :param workspace: workspace, defaults to None
    :type workspace: str, optional
    """

    def __init__(self, directory: str = SENTINEL, workspace: str = SENTINEL, **kwargs):
        """V2ProviderListLocation

        :param directory: directory, defaults to None
        :type directory: str, optional
        :param workspace: workspace, defaults to None
        :type workspace: str, optional
        """
        if directory is not SENTINEL:
            self.directory = self._define_str("directory", directory)
        if workspace is not SENTINEL:
            self.workspace = self._define_str("workspace", workspace)
        self._kwargs = kwargs
