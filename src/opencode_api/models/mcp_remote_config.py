# This file was generated by liblab | https://liblab.com/

from __future__ import annotations
from enum import Enum
from typing import Union
from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.one_of_base_model import OneOfBaseModel
from .utils.sentinel import SENTINEL
from .mcp_o_auth_config import McpOAuthConfig


class McpRemoteConfigType(Enum):
    """An enumeration representing different categories.

    :cvar REMOTE: "remote"
    :vartype REMOTE: str
    """

    REMOTE = "remote"

    def list():
        """Lists all category values.

        :return: A list of all category values.
        :rtype: list
        """
        return list(map(lambda x: x.value, McpRemoteConfigType._member_map_.values()))


class Oauth2(Enum):
    """An enumeration representing different categories.

    :cvar FALSE: "false"
    :vartype FALSE: str
    """

    FALSE = "false"

    def list():
        """Lists all category values.

        :return: A list of all category values.
        :rtype: list
        """
        return list(map(lambda x: x.value, Oauth2._member_map_.values()))


class OauthGuard(OneOfBaseModel):
    class_list = {"McpOAuthConfig": McpOAuthConfig, "Oauth2Enum": str}


Oauth = Union[McpOAuthConfig, str]


@JsonMap({"type_": "type"})
class McpRemoteConfig(BaseModel):
    """McpRemoteConfig

    :param type_: Type of MCP server connection
    :type type_: McpRemoteConfigType
    :param url: URL of the remote MCP server
    :type url: str
    :param enabled: enabled, defaults to None
    :type enabled: bool, optional
    :param headers: headers, defaults to None
    :type headers: dict, optional
    :param oauth: OAuth authentication configuration for the MCP server. Set to false to disable OAuth auto-detection., defaults to None
    :type oauth: Oauth, optional
    :param timeout: timeout, defaults to None
    :type timeout: int, optional
    """

    def __init__(
        self,
        type_: McpRemoteConfigType,
        url: str,
        enabled: bool = SENTINEL,
        headers: dict = SENTINEL,
        oauth: Oauth = SENTINEL,
        timeout: int = SENTINEL,
        **kwargs,
    ):
        """McpRemoteConfig

        :param type_: Type of MCP server connection
        :type type_: McpRemoteConfigType
        :param url: URL of the remote MCP server
        :type url: str
        :param enabled: enabled, defaults to None
        :type enabled: bool, optional
        :param headers: headers, defaults to None
        :type headers: dict, optional
        :param oauth: OAuth authentication configuration for the MCP server. Set to false to disable OAuth auto-detection., defaults to None
        :type oauth: Oauth, optional
        :param timeout: timeout, defaults to None
        :type timeout: int, optional
        """
        self.type_ = self._enum_matching(type_, McpRemoteConfigType.list(), "type_")
        self.url = self._define_str("url", url)
        if enabled is not SENTINEL:
            self.enabled = enabled
        if headers is not SENTINEL:
            self.headers = headers
        if oauth is not SENTINEL:
            self.oauth = OauthGuard.return_one_of(oauth)
        if timeout is not SENTINEL:
            self.timeout = self._define_number("timeout", timeout)
        self._kwargs = kwargs
