# This file was generated by liblab | https://liblab.com/

from __future__ import annotations
from typing import List
from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.sentinel import SENTINEL
from .question_info import QuestionInfo
from .question_tool import QuestionTool


@JsonMap({"id_": "id", "session_id": "sessionID"})
class QuestionRequest(BaseModel):
    """QuestionRequest

    :param id_: id_
    :type id_: str
    :param session_id: session_id
    :type session_id: str
    :param questions: Questions to ask
    :type questions: List[QuestionInfo]
    :param tool: tool, defaults to None
    :type tool: QuestionTool, optional
    """

    def __init__(
        self,
        id_: str,
        session_id: str,
        questions: List[QuestionInfo],
        tool: QuestionTool = SENTINEL,
        **kwargs,
    ):
        """QuestionRequest

        :param id_: id_
        :type id_: str
        :param session_id: session_id
        :type session_id: str
        :param questions: Questions to ask
        :type questions: List[QuestionInfo]
        :param tool: tool, defaults to None
        :type tool: QuestionTool, optional
        """
        self.id_ = self._define_str("id_", id_, pattern="^que")
        self.session_id = self._define_str("session_id", session_id, pattern="^ses")
        self.questions = self._define_list(questions, QuestionInfo)
        if tool is not SENTINEL:
            self.tool = self._define_object(tool, QuestionTool)
        self._kwargs = kwargs
