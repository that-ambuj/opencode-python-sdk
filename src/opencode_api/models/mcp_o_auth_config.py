# This file was generated by liblab | https://liblab.com/

from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.sentinel import SENTINEL


@JsonMap(
    {
        "client_id": "clientId",
        "client_secret": "clientSecret",
        "redirect_uri": "redirectUri",
    }
)
class McpOAuthConfig(BaseModel):
    """McpOAuthConfig

    :param client_id: client_id, defaults to None
    :type client_id: str, optional
    :param client_secret: client_secret, defaults to None
    :type client_secret: str, optional
    :param scope: scope, defaults to None
    :type scope: str, optional
    :param redirect_uri: redirect_uri, defaults to None
    :type redirect_uri: str, optional
    """

    def __init__(
        self,
        client_id: str = SENTINEL,
        client_secret: str = SENTINEL,
        scope: str = SENTINEL,
        redirect_uri: str = SENTINEL,
        **kwargs
    ):
        """McpOAuthConfig

        :param client_id: client_id, defaults to None
        :type client_id: str, optional
        :param client_secret: client_secret, defaults to None
        :type client_secret: str, optional
        :param scope: scope, defaults to None
        :type scope: str, optional
        :param redirect_uri: redirect_uri, defaults to None
        :type redirect_uri: str, optional
        """
        if client_id is not SENTINEL:
            self.client_id = self._define_str("client_id", client_id)
        if client_secret is not SENTINEL:
            self.client_secret = self._define_str("client_secret", client_secret)
        if scope is not SENTINEL:
            self.scope = self._define_str("scope", scope)
        if redirect_uri is not SENTINEL:
            self.redirect_uri = self._define_str("redirect_uri", redirect_uri)
        self._kwargs = kwargs
