# This file was generated by liblab | https://liblab.com/

from enum import Enum
from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.sentinel import SENTINEL


class PermissionReplyRequestReply(Enum):
    """An enumeration representing different categories.

    :cvar ONCE: "once"
    :vartype ONCE: str
    :cvar ALWAYS: "always"
    :vartype ALWAYS: str
    :cvar REJECT: "reject"
    :vartype REJECT: str
    """

    ONCE = "once"
    ALWAYS = "always"
    REJECT = "reject"

    def list():
        """Lists all category values.

        :return: A list of all category values.
        :rtype: list
        """
        return list(
            map(lambda x: x.value, PermissionReplyRequestReply._member_map_.values())
        )


@JsonMap({})
class PermissionReplyRequest(BaseModel):
    """PermissionReplyRequest

    :param reply: reply
    :type reply: PermissionReplyRequestReply
    :param message: message, defaults to None
    :type message: str, optional
    """

    def __init__(
        self, reply: PermissionReplyRequestReply, message: str = SENTINEL, **kwargs
    ):
        """PermissionReplyRequest

        :param reply: reply
        :type reply: PermissionReplyRequestReply
        :param message: message, defaults to None
        :type message: str, optional
        """
        self.reply = self._enum_matching(
            reply, PermissionReplyRequestReply.list(), "reply"
        )
        if message is not SENTINEL:
            self.message = self._define_str("message", message)
        self._kwargs = kwargs
