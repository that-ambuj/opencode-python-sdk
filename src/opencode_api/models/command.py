# This file was generated by liblab | https://liblab.com/

from enum import Enum
from typing import List
from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.sentinel import SENTINEL


class CommandSource(Enum):
    """An enumeration representing different categories.

    :cvar COMMAND: "command"
    :vartype COMMAND: str
    :cvar MCP: "mcp"
    :vartype MCP: str
    :cvar SKILL: "skill"
    :vartype SKILL: str
    """

    COMMAND = "command"
    MCP = "mcp"
    SKILL = "skill"

    def list():
        """Lists all category values.

        :return: A list of all category values.
        :rtype: list
        """
        return list(map(lambda x: x.value, CommandSource._member_map_.values()))


@JsonMap({})
class Command(BaseModel):
    """Command

    :param name: name
    :type name: str
    :param description: description, defaults to None
    :type description: str, optional
    :param agent: agent, defaults to None
    :type agent: str, optional
    :param model: model, defaults to None
    :type model: str, optional
    :param source: source, defaults to None
    :type source: CommandSource, optional
    :param template: template
    :type template: str
    :param subtask: subtask, defaults to None
    :type subtask: bool, optional
    :param hints: hints
    :type hints: List[str]
    """

    def __init__(
        self,
        name: str,
        template: str,
        hints: List[str],
        description: str = SENTINEL,
        agent: str = SENTINEL,
        model: str = SENTINEL,
        source: CommandSource = SENTINEL,
        subtask: bool = SENTINEL,
        **kwargs
    ):
        """Command

        :param name: name
        :type name: str
        :param description: description, defaults to None
        :type description: str, optional
        :param agent: agent, defaults to None
        :type agent: str, optional
        :param model: model, defaults to None
        :type model: str, optional
        :param source: source, defaults to None
        :type source: CommandSource, optional
        :param template: template
        :type template: str
        :param subtask: subtask, defaults to None
        :type subtask: bool, optional
        :param hints: hints
        :type hints: List[str]
        """
        self.name = self._define_str("name", name)
        if description is not SENTINEL:
            self.description = self._define_str("description", description)
        if agent is not SENTINEL:
            self.agent = self._define_str("agent", agent)
        if model is not SENTINEL:
            self.model = self._define_str("model", model)
        if source is not SENTINEL:
            self.source = self._enum_matching(source, CommandSource.list(), "source")
        self.template = self._define_str("template", template)
        if subtask is not SENTINEL:
            self.subtask = subtask
        self.hints = hints
        self._kwargs = kwargs
