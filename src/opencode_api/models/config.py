# This file was generated by liblab | https://liblab.com/

from __future__ import annotations
from enum import Enum
from typing import Union
from typing import List
from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.one_of_base_model import OneOfBaseModel
from .utils.sentinel import SENTINEL
from .log_level import LogLevel
from .server_config import ServerConfig
from .reference_config_entry import ReferenceConfigEntry, ReferenceConfigEntryGuard
from .provider_config import ProviderConfig
from .layout_config import LayoutConfig
from .permission_config import PermissionConfig, PermissionConfigGuard
from .permission_action_config import PermissionActionConfig
from .attachment_config import AttachmentConfig
from .agent_config import AgentConfig
from .mcp_local_config import McpLocalConfig
from .mcp_remote_config import McpRemoteConfig


@JsonMap({"schema": "$schema", "log_level": "logLevel"})
class Config(BaseModel):
    """Config

    :param schema: schema, defaults to None
    :type schema: str, optional
    :param shell: shell, defaults to None
    :type shell: str, optional
    :param log_level: Log level, defaults to None
    :type log_level: LogLevel, optional
    :param server: Server configuration for opencode serve and web commands, defaults to None
    :type server: ServerConfig, optional
    :param command: command, defaults to None
    :type command: dict, optional
    :param skills: skills, defaults to None
    :type skills: Skills, optional
    :param reference: reference, defaults to None
    :type reference: dict, optional
    :param watcher: watcher, defaults to None
    :type watcher: Watcher, optional
    :param snapshot: snapshot, defaults to None
    :type snapshot: bool, optional
    :param plugin: plugin, defaults to None
    :type plugin: List[Plugin], optional
    :param share: share, defaults to None
    :type share: ConfigShare, optional
    :param autoshare: autoshare, defaults to None
    :type autoshare: bool, optional
    :param autoupdate: Automatically update to the latest version. Set to true to auto-update, false to disable, or 'notify' to show update notifications, defaults to None
    :type autoupdate: Autoupdate, optional
    :param disabled_providers: disabled_providers, defaults to None
    :type disabled_providers: List[str], optional
    :param enabled_providers: enabled_providers, defaults to None
    :type enabled_providers: List[str], optional
    :param model: model, defaults to None
    :type model: str, optional
    :param small_model: small_model, defaults to None
    :type small_model: str, optional
    :param default_agent: default_agent, defaults to None
    :type default_agent: str, optional
    :param username: username, defaults to None
    :type username: str, optional
    :param mode: mode, defaults to None
    :type mode: ConfigMode, optional
    :param agent: agent, defaults to None
    :type agent: ConfigAgent, optional
    :param provider: provider, defaults to None
    :type provider: dict, optional
    :param mcp: mcp, defaults to None
    :type mcp: dict, optional
    :param formatter: Enable or configure formatters. Omit or set to false to disable, true to enable built-ins, or an object to enable built-ins with overrides., defaults to None
    :type formatter: Formatter, optional
    :param lsp: Enable or configure LSP servers. Omit or set to false to disable, true to enable built-ins, or an object to enable built-ins with overrides., defaults to None
    :type lsp: Lsp, optional
    :param instructions: instructions, defaults to None
    :type instructions: List[str], optional
    :param layout: @deprecated Always uses stretch layout., defaults to None
    :type layout: LayoutConfig, optional
    :param permission: permission, defaults to None
    :type permission: PermissionConfig, optional
    :param tools: tools, defaults to None
    :type tools: dict, optional
    :param attachment: attachment, defaults to None
    :type attachment: AttachmentConfig, optional
    :param enterprise: enterprise, defaults to None
    :type enterprise: Enterprise, optional
    :param tool_output: tool_output, defaults to None
    :type tool_output: ToolOutput, optional
    :param compaction: compaction, defaults to None
    :type compaction: Compaction, optional
    :param experimental: experimental, defaults to None
    :type experimental: Experimental, optional
    """

    def __init__(
        self,
        schema: str = SENTINEL,
        shell: str = SENTINEL,
        log_level: LogLevel = SENTINEL,
        server: ServerConfig = SENTINEL,
        command: dict = SENTINEL,
        skills: Skills = SENTINEL,
        reference: dict = SENTINEL,
        watcher: Watcher = SENTINEL,
        snapshot: bool = SENTINEL,
        plugin: List[Plugin] = SENTINEL,
        share: ConfigShare = SENTINEL,
        autoshare: bool = SENTINEL,
        autoupdate: Autoupdate = SENTINEL,
        disabled_providers: List[str] = SENTINEL,
        enabled_providers: List[str] = SENTINEL,
        model: str = SENTINEL,
        small_model: str = SENTINEL,
        default_agent: str = SENTINEL,
        username: str = SENTINEL,
        mode: ConfigMode = SENTINEL,
        agent: ConfigAgent = SENTINEL,
        provider: dict = SENTINEL,
        mcp: dict = SENTINEL,
        formatter: Formatter = SENTINEL,
        lsp: Lsp = SENTINEL,
        instructions: List[str] = SENTINEL,
        layout: LayoutConfig = SENTINEL,
        permission: PermissionConfig = SENTINEL,
        tools: dict = SENTINEL,
        attachment: AttachmentConfig = SENTINEL,
        enterprise: Enterprise = SENTINEL,
        tool_output: ToolOutput = SENTINEL,
        compaction: Compaction = SENTINEL,
        experimental: Experimental = SENTINEL,
        **kwargs,
    ):
        """Config

        :param schema: schema, defaults to None
        :type schema: str, optional
        :param shell: shell, defaults to None
        :type shell: str, optional
        :param log_level: Log level, defaults to None
        :type log_level: LogLevel, optional
        :param server: Server configuration for opencode serve and web commands, defaults to None
        :type server: ServerConfig, optional
        :param command: command, defaults to None
        :type command: dict, optional
        :param skills: skills, defaults to None
        :type skills: Skills, optional
        :param reference: reference, defaults to None
        :type reference: dict, optional
        :param watcher: watcher, defaults to None
        :type watcher: Watcher, optional
        :param snapshot: snapshot, defaults to None
        :type snapshot: bool, optional
        :param plugin: plugin, defaults to None
        :type plugin: List[Plugin], optional
        :param share: share, defaults to None
        :type share: ConfigShare, optional
        :param autoshare: autoshare, defaults to None
        :type autoshare: bool, optional
        :param autoupdate: Automatically update to the latest version. Set to true to auto-update, false to disable, or 'notify' to show update notifications, defaults to None
        :type autoupdate: Autoupdate, optional
        :param disabled_providers: disabled_providers, defaults to None
        :type disabled_providers: List[str], optional
        :param enabled_providers: enabled_providers, defaults to None
        :type enabled_providers: List[str], optional
        :param model: model, defaults to None
        :type model: str, optional
        :param small_model: small_model, defaults to None
        :type small_model: str, optional
        :param default_agent: default_agent, defaults to None
        :type default_agent: str, optional
        :param username: username, defaults to None
        :type username: str, optional
        :param mode: mode, defaults to None
        :type mode: ConfigMode, optional
        :param agent: agent, defaults to None
        :type agent: ConfigAgent, optional
        :param provider: provider, defaults to None
        :type provider: dict, optional
        :param mcp: mcp, defaults to None
        :type mcp: dict, optional
        :param formatter: Enable or configure formatters. Omit or set to false to disable, true to enable built-ins, or an object to enable built-ins with overrides., defaults to None
        :type formatter: Formatter, optional
        :param lsp: Enable or configure LSP servers. Omit or set to false to disable, true to enable built-ins, or an object to enable built-ins with overrides., defaults to None
        :type lsp: Lsp, optional
        :param instructions: instructions, defaults to None
        :type instructions: List[str], optional
        :param layout: @deprecated Always uses stretch layout., defaults to None
        :type layout: LayoutConfig, optional
        :param permission: permission, defaults to None
        :type permission: PermissionConfig, optional
        :param tools: tools, defaults to None
        :type tools: dict, optional
        :param attachment: attachment, defaults to None
        :type attachment: AttachmentConfig, optional
        :param enterprise: enterprise, defaults to None
        :type enterprise: Enterprise, optional
        :param tool_output: tool_output, defaults to None
        :type tool_output: ToolOutput, optional
        :param compaction: compaction, defaults to None
        :type compaction: Compaction, optional
        :param experimental: experimental, defaults to None
        :type experimental: Experimental, optional
        """
        if schema is not SENTINEL:
            self.schema = self._define_str("schema", schema)
        if shell is not SENTINEL:
            self.shell = self._define_str("shell", shell)
        if log_level is not SENTINEL:
            self.log_level = self._enum_matching(
                log_level, LogLevel.list(), "log_level"
            )
        if server is not SENTINEL:
            self.server = self._define_object(server, ServerConfig)
        if command is not SENTINEL:
            self.command = command
        if skills is not SENTINEL:
            self.skills = self._define_object(skills, Skills)
        if reference is not SENTINEL:
            self.reference = reference
        if watcher is not SENTINEL:
            self.watcher = self._define_object(watcher, Watcher)
        if snapshot is not SENTINEL:
            self.snapshot = snapshot
        if plugin is not SENTINEL:
            self.plugin = self._define_list(plugin, Plugin)
        if share is not SENTINEL:
            self.share = self._enum_matching(share, ConfigShare.list(), "share")
        if autoshare is not SENTINEL:
            self.autoshare = autoshare
        if autoupdate is not SENTINEL:
            self.autoupdate = AutoupdateGuard.return_one_of(autoupdate)
        if disabled_providers is not SENTINEL:
            self.disabled_providers = disabled_providers
        if enabled_providers is not SENTINEL:
            self.enabled_providers = enabled_providers
        if model is not SENTINEL:
            self.model = self._define_str("model", model)
        if small_model is not SENTINEL:
            self.small_model = self._define_str("small_model", small_model)
        if default_agent is not SENTINEL:
            self.default_agent = self._define_str("default_agent", default_agent)
        if username is not SENTINEL:
            self.username = self._define_str("username", username)
        if mode is not SENTINEL:
            self.mode = self._define_object(mode, ConfigMode)
        if agent is not SENTINEL:
            self.agent = self._define_object(agent, ConfigAgent)
        if provider is not SENTINEL:
            self.provider = provider
        if mcp is not SENTINEL:
            self.mcp = mcp
        if formatter is not SENTINEL:
            self.formatter = FormatterGuard.return_one_of(formatter)
        if lsp is not SENTINEL:
            self.lsp = LspGuard.return_one_of(lsp)
        if instructions is not SENTINEL:
            self.instructions = instructions
        if layout is not SENTINEL:
            self.layout = self._enum_matching(layout, LayoutConfig.list(), "layout")
        if permission is not SENTINEL:
            self.permission = PermissionConfigGuard.return_one_of(permission)
        if tools is not SENTINEL:
            self.tools = tools
        if attachment is not SENTINEL:
            self.attachment = self._define_object(attachment, AttachmentConfig)
        if enterprise is not SENTINEL:
            self.enterprise = self._define_object(enterprise, Enterprise)
        if tool_output is not SENTINEL:
            self.tool_output = self._define_object(tool_output, ToolOutput)
        if compaction is not SENTINEL:
            self.compaction = self._define_object(compaction, Compaction)
        if experimental is not SENTINEL:
            self.experimental = self._define_object(experimental, Experimental)
        self._kwargs = kwargs


@JsonMap({})
class ConfigCommand(BaseModel):
    """ConfigCommand

    :param template: template
    :type template: str
    :param description: description, defaults to None
    :type description: str, optional
    :param agent: agent, defaults to None
    :type agent: str, optional
    :param model: model, defaults to None
    :type model: str, optional
    :param subtask: subtask, defaults to None
    :type subtask: bool, optional
    """

    def __init__(
        self,
        template: str,
        description: str = SENTINEL,
        agent: str = SENTINEL,
        model: str = SENTINEL,
        subtask: bool = SENTINEL,
        **kwargs,
    ):
        """ConfigCommand

        :param template: template
        :type template: str
        :param description: description, defaults to None
        :type description: str, optional
        :param agent: agent, defaults to None
        :type agent: str, optional
        :param model: model, defaults to None
        :type model: str, optional
        :param subtask: subtask, defaults to None
        :type subtask: bool, optional
        """
        self.template = self._define_str("template", template)
        if description is not SENTINEL:
            self.description = self._define_str("description", description)
        if agent is not SENTINEL:
            self.agent = self._define_str("agent", agent)
        if model is not SENTINEL:
            self.model = self._define_str("model", model)
        if subtask is not SENTINEL:
            self.subtask = subtask
        self._kwargs = kwargs


@JsonMap({})
class Skills(BaseModel):
    """Skills

    :param paths: paths, defaults to None
    :type paths: List[str], optional
    :param urls: urls, defaults to None
    :type urls: List[str], optional
    """

    def __init__(
        self, paths: List[str] = SENTINEL, urls: List[str] = SENTINEL, **kwargs
    ):
        """Skills

        :param paths: paths, defaults to None
        :type paths: List[str], optional
        :param urls: urls, defaults to None
        :type urls: List[str], optional
        """
        if paths is not SENTINEL:
            self.paths = paths
        if urls is not SENTINEL:
            self.urls = urls
        self._kwargs = kwargs


@JsonMap({})
class Watcher(BaseModel):
    """Watcher

    :param ignore: ignore, defaults to None
    :type ignore: List[str], optional
    """

    def __init__(self, ignore: List[str] = SENTINEL, **kwargs):
        """Watcher

        :param ignore: ignore, defaults to None
        :type ignore: List[str], optional
        """
        if ignore is not SENTINEL:
            self.ignore = ignore
        self._kwargs = kwargs


class PluginGuard(OneOfBaseModel):
    class_list = {"str": str, "List[dict]": List[dict]}


Plugin = Union[str, List[dict]]


class ConfigShare(Enum):
    """An enumeration representing different categories.

    :cvar MANUAL: "manual"
    :vartype MANUAL: str
    :cvar AUTO: "auto"
    :vartype AUTO: str
    :cvar DISABLED: "disabled"
    :vartype DISABLED: str
    """

    MANUAL = "manual"
    AUTO = "auto"
    DISABLED = "disabled"

    def list():
        """Lists all category values.

        :return: A list of all category values.
        :rtype: list
        """
        return list(map(lambda x: x.value, ConfigShare._member_map_.values()))


class Autoupdate2(Enum):
    """An enumeration representing different categories.

    :cvar NOTIFY: "notify"
    :vartype NOTIFY: str
    """

    NOTIFY = "notify"

    def list():
        """Lists all category values.

        :return: A list of all category values.
        :rtype: list
        """
        return list(map(lambda x: x.value, Autoupdate2._member_map_.values()))


class AutoupdateGuard(OneOfBaseModel):
    class_list = {"bool": bool, "Autoupdate2Enum": str}


Autoupdate = Union[bool, str]


@JsonMap({})
class ConfigMode(BaseModel):
    """ConfigMode

    :param build: build, defaults to None
    :type build: AgentConfig, optional
    :param plan: plan, defaults to None
    :type plan: AgentConfig, optional
    """

    def __init__(
        self, build: AgentConfig = SENTINEL, plan: AgentConfig = SENTINEL, **kwargs
    ):
        """ConfigMode

        :param build: build, defaults to None
        :type build: AgentConfig, optional
        :param plan: plan, defaults to None
        :type plan: AgentConfig, optional
        """
        if build is not SENTINEL:
            self.build = self._define_object(build, AgentConfig)
        if plan is not SENTINEL:
            self.plan = self._define_object(plan, AgentConfig)
        self._kwargs = kwargs


@JsonMap({})
class ConfigAgent(BaseModel):
    """ConfigAgent

    :param plan: plan, defaults to None
    :type plan: AgentConfig, optional
    :param build: build, defaults to None
    :type build: AgentConfig, optional
    :param general: general, defaults to None
    :type general: AgentConfig, optional
    :param explore: explore, defaults to None
    :type explore: AgentConfig, optional
    :param scout: scout, defaults to None
    :type scout: AgentConfig, optional
    :param title: title, defaults to None
    :type title: AgentConfig, optional
    :param summary: summary, defaults to None
    :type summary: AgentConfig, optional
    :param compaction: compaction, defaults to None
    :type compaction: AgentConfig, optional
    """

    def __init__(
        self,
        plan: AgentConfig = SENTINEL,
        build: AgentConfig = SENTINEL,
        general: AgentConfig = SENTINEL,
        explore: AgentConfig = SENTINEL,
        scout: AgentConfig = SENTINEL,
        title: AgentConfig = SENTINEL,
        summary: AgentConfig = SENTINEL,
        compaction: AgentConfig = SENTINEL,
        **kwargs,
    ):
        """ConfigAgent

        :param plan: plan, defaults to None
        :type plan: AgentConfig, optional
        :param build: build, defaults to None
        :type build: AgentConfig, optional
        :param general: general, defaults to None
        :type general: AgentConfig, optional
        :param explore: explore, defaults to None
        :type explore: AgentConfig, optional
        :param scout: scout, defaults to None
        :type scout: AgentConfig, optional
        :param title: title, defaults to None
        :type title: AgentConfig, optional
        :param summary: summary, defaults to None
        :type summary: AgentConfig, optional
        :param compaction: compaction, defaults to None
        :type compaction: AgentConfig, optional
        """
        if plan is not SENTINEL:
            self.plan = self._define_object(plan, AgentConfig)
        if build is not SENTINEL:
            self.build = self._define_object(build, AgentConfig)
        if general is not SENTINEL:
            self.general = self._define_object(general, AgentConfig)
        if explore is not SENTINEL:
            self.explore = self._define_object(explore, AgentConfig)
        if scout is not SENTINEL:
            self.scout = self._define_object(scout, AgentConfig)
        if title is not SENTINEL:
            self.title = self._define_object(title, AgentConfig)
        if summary is not SENTINEL:
            self.summary = self._define_object(summary, AgentConfig)
        if compaction is not SENTINEL:
            self.compaction = self._define_object(compaction, AgentConfig)
        self._kwargs = kwargs


@JsonMap({})
class Mcp3(BaseModel):
    """Mcp3

    :param enabled: enabled
    :type enabled: bool
    """

    def __init__(self, enabled: bool, **kwargs):
        """Mcp3

        :param enabled: enabled
        :type enabled: bool
        """
        self.enabled = enabled
        self._kwargs = kwargs


class McpGuard(OneOfBaseModel):
    class_list = {
        "McpLocalConfig": McpLocalConfig,
        "McpRemoteConfig": McpRemoteConfig,
        "Mcp3": Mcp3,
    }


Mcp = Union[McpLocalConfig, McpRemoteConfig, Mcp3]


@JsonMap({})
class Formatter2(BaseModel):
    """Formatter2

    :param disabled: disabled, defaults to None
    :type disabled: bool, optional
    :param command: command, defaults to None
    :type command: List[str], optional
    :param environment: environment, defaults to None
    :type environment: dict, optional
    :param extensions: extensions, defaults to None
    :type extensions: List[str], optional
    """

    def __init__(
        self,
        disabled: bool = SENTINEL,
        command: List[str] = SENTINEL,
        environment: dict = SENTINEL,
        extensions: List[str] = SENTINEL,
        **kwargs,
    ):
        """Formatter2

        :param disabled: disabled, defaults to None
        :type disabled: bool, optional
        :param command: command, defaults to None
        :type command: List[str], optional
        :param environment: environment, defaults to None
        :type environment: dict, optional
        :param extensions: extensions, defaults to None
        :type extensions: List[str], optional
        """
        if disabled is not SENTINEL:
            self.disabled = disabled
        if command is not SENTINEL:
            self.command = command
        if environment is not SENTINEL:
            self.environment = environment
        if extensions is not SENTINEL:
            self.extensions = extensions
        self._kwargs = kwargs


class FormatterGuard(OneOfBaseModel):
    class_list = {"bool": bool, "dict": dict}


Formatter = Union[bool, dict]


class Disabled(Enum):
    """An enumeration representing different categories.

    :cvar TRUE: "true"
    :vartype TRUE: str
    """

    TRUE = "true"

    def list():
        """Lists all category values.

        :return: A list of all category values.
        :rtype: list
        """
        return list(map(lambda x: x.value, Disabled._member_map_.values()))


@JsonMap({})
class Lsp2_1(BaseModel):
    """Lsp2_1

    :param disabled: disabled
    :type disabled: Disabled
    """

    def __init__(self, disabled: Disabled, **kwargs):
        """Lsp2_1

        :param disabled: disabled
        :type disabled: Disabled
        """
        self.disabled = self._enum_matching(disabled, Disabled.list(), "disabled")
        self._kwargs = kwargs


@JsonMap({})
class Lsp2_2(BaseModel):
    """Lsp2_2

    :param command: command
    :type command: List[str]
    :param extensions: extensions, defaults to None
    :type extensions: List[str], optional
    :param disabled: disabled, defaults to None
    :type disabled: bool, optional
    :param env: env, defaults to None
    :type env: dict, optional
    :param initialization: initialization, defaults to None
    :type initialization: dict, optional
    """

    def __init__(
        self,
        command: List[str],
        extensions: List[str] = SENTINEL,
        disabled: bool = SENTINEL,
        env: dict = SENTINEL,
        initialization: dict = SENTINEL,
        **kwargs,
    ):
        """Lsp2_2

        :param command: command
        :type command: List[str]
        :param extensions: extensions, defaults to None
        :type extensions: List[str], optional
        :param disabled: disabled, defaults to None
        :type disabled: bool, optional
        :param env: env, defaults to None
        :type env: dict, optional
        :param initialization: initialization, defaults to None
        :type initialization: dict, optional
        """
        self.command = command
        if extensions is not SENTINEL:
            self.extensions = extensions
        if disabled is not SENTINEL:
            self.disabled = disabled
        if env is not SENTINEL:
            self.env = env
        if initialization is not SENTINEL:
            self.initialization = initialization
        self._kwargs = kwargs


class Lsp2Guard(OneOfBaseModel):
    class_list = {"Lsp2_1": Lsp2_1, "Lsp2_2": Lsp2_2}


Lsp2 = Union[Lsp2_1, Lsp2_2]


class LspGuard(OneOfBaseModel):
    class_list = {"bool": bool, "dict": dict}


Lsp = Union[bool, dict]


@JsonMap({})
class Enterprise(BaseModel):
    """Enterprise

    :param url: url, defaults to None
    :type url: str, optional
    """

    def __init__(self, url: str = SENTINEL, **kwargs):
        """Enterprise

        :param url: url, defaults to None
        :type url: str, optional
        """
        if url is not SENTINEL:
            self.url = self._define_str("url", url)
        self._kwargs = kwargs


@JsonMap({})
class ToolOutput(BaseModel):
    """ToolOutput

    :param max_lines: max_lines, defaults to None
    :type max_lines: int, optional
    :param max_bytes: max_bytes, defaults to None
    :type max_bytes: int, optional
    """

    def __init__(self, max_lines: int = SENTINEL, max_bytes: int = SENTINEL, **kwargs):
        """ToolOutput

        :param max_lines: max_lines, defaults to None
        :type max_lines: int, optional
        :param max_bytes: max_bytes, defaults to None
        :type max_bytes: int, optional
        """
        if max_lines is not SENTINEL:
            self.max_lines = self._define_number("max_lines", max_lines)
        if max_bytes is not SENTINEL:
            self.max_bytes = self._define_number("max_bytes", max_bytes)
        self._kwargs = kwargs


@JsonMap({})
class Compaction(BaseModel):
    """Compaction

    :param auto: auto, defaults to None
    :type auto: bool, optional
    :param prune: prune, defaults to None
    :type prune: bool, optional
    :param tail_turns: tail_turns, defaults to None
    :type tail_turns: int, optional
    :param preserve_recent_tokens: preserve_recent_tokens, defaults to None
    :type preserve_recent_tokens: int, optional
    :param reserved: reserved, defaults to None
    :type reserved: int, optional
    """

    def __init__(
        self,
        auto: bool = SENTINEL,
        prune: bool = SENTINEL,
        tail_turns: int = SENTINEL,
        preserve_recent_tokens: int = SENTINEL,
        reserved: int = SENTINEL,
        **kwargs,
    ):
        """Compaction

        :param auto: auto, defaults to None
        :type auto: bool, optional
        :param prune: prune, defaults to None
        :type prune: bool, optional
        :param tail_turns: tail_turns, defaults to None
        :type tail_turns: int, optional
        :param preserve_recent_tokens: preserve_recent_tokens, defaults to None
        :type preserve_recent_tokens: int, optional
        :param reserved: reserved, defaults to None
        :type reserved: int, optional
        """
        if auto is not SENTINEL:
            self.auto = auto
        if prune is not SENTINEL:
            self.prune = prune
        if tail_turns is not SENTINEL:
            self.tail_turns = self._define_number("tail_turns", tail_turns, ge=0)
        if preserve_recent_tokens is not SENTINEL:
            self.preserve_recent_tokens = self._define_number(
                "preserve_recent_tokens", preserve_recent_tokens, ge=0
            )
        if reserved is not SENTINEL:
            self.reserved = self._define_number("reserved", reserved, ge=0)
        self._kwargs = kwargs


@JsonMap({"open_telemetry": "openTelemetry"})
class Experimental(BaseModel):
    """Experimental

    :param disable_paste_summary: disable_paste_summary, defaults to None
    :type disable_paste_summary: bool, optional
    :param batch_tool: batch_tool, defaults to None
    :type batch_tool: bool, optional
    :param open_telemetry: open_telemetry, defaults to None
    :type open_telemetry: bool, optional
    :param primary_tools: primary_tools, defaults to None
    :type primary_tools: List[str], optional
    :param continue_loop_on_deny: continue_loop_on_deny, defaults to None
    :type continue_loop_on_deny: bool, optional
    :param mcp_timeout: mcp_timeout, defaults to None
    :type mcp_timeout: int, optional
    """

    def __init__(
        self,
        disable_paste_summary: bool = SENTINEL,
        batch_tool: bool = SENTINEL,
        open_telemetry: bool = SENTINEL,
        primary_tools: List[str] = SENTINEL,
        continue_loop_on_deny: bool = SENTINEL,
        mcp_timeout: int = SENTINEL,
        **kwargs,
    ):
        """Experimental

        :param disable_paste_summary: disable_paste_summary, defaults to None
        :type disable_paste_summary: bool, optional
        :param batch_tool: batch_tool, defaults to None
        :type batch_tool: bool, optional
        :param open_telemetry: open_telemetry, defaults to None
        :type open_telemetry: bool, optional
        :param primary_tools: primary_tools, defaults to None
        :type primary_tools: List[str], optional
        :param continue_loop_on_deny: continue_loop_on_deny, defaults to None
        :type continue_loop_on_deny: bool, optional
        :param mcp_timeout: mcp_timeout, defaults to None
        :type mcp_timeout: int, optional
        """
        if disable_paste_summary is not SENTINEL:
            self.disable_paste_summary = disable_paste_summary
        if batch_tool is not SENTINEL:
            self.batch_tool = batch_tool
        if open_telemetry is not SENTINEL:
            self.open_telemetry = open_telemetry
        if primary_tools is not SENTINEL:
            self.primary_tools = primary_tools
        if continue_loop_on_deny is not SENTINEL:
            self.continue_loop_on_deny = continue_loop_on_deny
        if mcp_timeout is not SENTINEL:
            self.mcp_timeout = self._define_number("mcp_timeout", mcp_timeout)
        self._kwargs = kwargs
