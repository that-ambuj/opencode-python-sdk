# This file was generated by liblab | https://liblab.com/

from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.sentinel import SENTINEL


@JsonMap({})
class Worktree(BaseModel):
    """Worktree

    :param name: name
    :type name: str
    :param branch: branch, defaults to None
    :type branch: str, optional
    :param directory: directory
    :type directory: str
    """

    def __init__(self, name: str, directory: str, branch: str = SENTINEL, **kwargs):
        """Worktree

        :param name: name
        :type name: str
        :param branch: branch, defaults to None
        :type branch: str, optional
        :param directory: directory
        :type directory: str
        """
        self.name = self._define_str("name", name)
        if branch is not SENTINEL:
            self.branch = self._define_str("branch", branch)
        self.directory = self._define_str("directory", directory)
        self._kwargs = kwargs
