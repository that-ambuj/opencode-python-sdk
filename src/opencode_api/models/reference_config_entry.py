# This file was generated by liblab | https://liblab.com/

from typing import Union
from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.one_of_base_model import OneOfBaseModel
from .utils.sentinel import SENTINEL


@JsonMap({})
class ReferenceConfigEntry2(BaseModel):
    """ReferenceConfigEntry2

    :param repository: Git repository URL, host/path reference, or GitHub owner/repo shorthand
    :type repository: str
    :param branch: branch, defaults to None
    :type branch: str, optional
    """

    def __init__(self, repository: str, branch: str = SENTINEL, **kwargs):
        """ReferenceConfigEntry2

        :param repository: Git repository URL, host/path reference, or GitHub owner/repo shorthand
        :type repository: str
        :param branch: branch, defaults to None
        :type branch: str, optional
        """
        self.repository = self._define_str("repository", repository)
        if branch is not SENTINEL:
            self.branch = self._define_str("branch", branch)
        self._kwargs = kwargs


@JsonMap({})
class ReferenceConfigEntry3(BaseModel):
    """ReferenceConfigEntry3

    :param path: Absolute path, ~/ path, or workspace-relative path to a local reference directory
    :type path: str
    """

    def __init__(self, path: str, **kwargs):
        """ReferenceConfigEntry3

        :param path: Absolute path, ~/ path, or workspace-relative path to a local reference directory
        :type path: str
        """
        self.path = self._define_str("path", path)
        self._kwargs = kwargs


class ReferenceConfigEntryGuard(OneOfBaseModel):
    class_list = {
        "str": str,
        "ReferenceConfigEntry2": ReferenceConfigEntry2,
        "ReferenceConfigEntry3": ReferenceConfigEntry3,
    }


ReferenceConfigEntry = Union[str, ReferenceConfigEntry2, ReferenceConfigEntry3]
