# This file was generated by liblab | https://liblab.com/

from typing import List
from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.sentinel import SENTINEL


@JsonMap({"mdns_domain": "mdnsDomain"})
class ServerConfig(BaseModel):
    """Server configuration for opencode serve and web commands

    :param port: port, defaults to None
    :type port: int, optional
    :param hostname: hostname, defaults to None
    :type hostname: str, optional
    :param mdns: mdns, defaults to None
    :type mdns: bool, optional
    :param mdns_domain: mdns_domain, defaults to None
    :type mdns_domain: str, optional
    :param cors: cors, defaults to None
    :type cors: List[str], optional
    """

    def __init__(
        self,
        port: int = SENTINEL,
        hostname: str = SENTINEL,
        mdns: bool = SENTINEL,
        mdns_domain: str = SENTINEL,
        cors: List[str] = SENTINEL,
        **kwargs
    ):
        """Server configuration for opencode serve and web commands

        :param port: port, defaults to None
        :type port: int, optional
        :param hostname: hostname, defaults to None
        :type hostname: str, optional
        :param mdns: mdns, defaults to None
        :type mdns: bool, optional
        :param mdns_domain: mdns_domain, defaults to None
        :type mdns_domain: str, optional
        :param cors: cors, defaults to None
        :type cors: List[str], optional
        """
        if port is not SENTINEL:
            self.port = self._define_number("port", port)
        if hostname is not SENTINEL:
            self.hostname = self._define_str("hostname", hostname)
        if mdns is not SENTINEL:
            self.mdns = mdns
        if mdns_domain is not SENTINEL:
            self.mdns_domain = self._define_str("mdns_domain", mdns_domain)
        if cors is not SENTINEL:
            self.cors = cors
        self._kwargs = kwargs
