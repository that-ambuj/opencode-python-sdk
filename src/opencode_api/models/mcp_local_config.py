# This file was generated by liblab | https://liblab.com/

from enum import Enum
from typing import List
from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.sentinel import SENTINEL


class McpLocalConfigType(Enum):
    """An enumeration representing different categories.

    :cvar LOCAL: "local"
    :vartype LOCAL: str
    """

    LOCAL = "local"

    def list():
        """Lists all category values.

        :return: A list of all category values.
        :rtype: list
        """
        return list(map(lambda x: x.value, McpLocalConfigType._member_map_.values()))


@JsonMap({"type_": "type"})
class McpLocalConfig(BaseModel):
    """McpLocalConfig

    :param type_: Type of MCP server connection
    :type type_: McpLocalConfigType
    :param command: Command and arguments to run the MCP server
    :type command: List[str]
    :param environment: environment, defaults to None
    :type environment: dict, optional
    :param enabled: enabled, defaults to None
    :type enabled: bool, optional
    :param timeout: timeout, defaults to None
    :type timeout: int, optional
    """

    def __init__(
        self,
        type_: McpLocalConfigType,
        command: List[str],
        environment: dict = SENTINEL,
        enabled: bool = SENTINEL,
        timeout: int = SENTINEL,
        **kwargs
    ):
        """McpLocalConfig

        :param type_: Type of MCP server connection
        :type type_: McpLocalConfigType
        :param command: Command and arguments to run the MCP server
        :type command: List[str]
        :param environment: environment, defaults to None
        :type environment: dict, optional
        :param enabled: enabled, defaults to None
        :type enabled: bool, optional
        :param timeout: timeout, defaults to None
        :type timeout: int, optional
        """
        self.type_ = self._enum_matching(type_, McpLocalConfigType.list(), "type_")
        self.command = command
        if environment is not SENTINEL:
            self.environment = environment
        if enabled is not SENTINEL:
            self.enabled = enabled
        if timeout is not SENTINEL:
            self.timeout = self._define_number("timeout", timeout)
        self._kwargs = kwargs
