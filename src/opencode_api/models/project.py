# This file was generated by liblab | https://liblab.com/

from enum import Enum
from typing import List
from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.sentinel import SENTINEL


class Vcs(Enum):
    """An enumeration representing different categories.

    :cvar GIT: "git"
    :vartype GIT: str
    """

    GIT = "git"

    def list():
        """Lists all category values.

        :return: A list of all category values.
        :rtype: list
        """
        return list(map(lambda x: x.value, Vcs._member_map_.values()))


@JsonMap({})
class ProjectIcon(BaseModel):
    """ProjectIcon

    :param url: url, defaults to None
    :type url: str, optional
    :param override: override, defaults to None
    :type override: str, optional
    :param color: color, defaults to None
    :type color: str, optional
    """

    def __init__(
        self,
        url: str = SENTINEL,
        override: str = SENTINEL,
        color: str = SENTINEL,
        **kwargs
    ):
        """ProjectIcon

        :param url: url, defaults to None
        :type url: str, optional
        :param override: override, defaults to None
        :type override: str, optional
        :param color: color, defaults to None
        :type color: str, optional
        """
        if url is not SENTINEL:
            self.url = self._define_str("url", url)
        if override is not SENTINEL:
            self.override = self._define_str("override", override)
        if color is not SENTINEL:
            self.color = self._define_str("color", color)
        self._kwargs = kwargs


@JsonMap({})
class ProjectCommands(BaseModel):
    """ProjectCommands

    :param start: Startup script to run when creating a new workspace (worktree), defaults to None
    :type start: str, optional
    """

    def __init__(self, start: str = SENTINEL, **kwargs):
        """ProjectCommands

        :param start: Startup script to run when creating a new workspace (worktree), defaults to None
        :type start: str, optional
        """
        if start is not SENTINEL:
            self.start = self._define_str("start", start)
        self._kwargs = kwargs


@JsonMap({})
class ProjectTime(BaseModel):
    """ProjectTime

    :param created: created
    :type created: int
    :param updated: updated
    :type updated: int
    :param initialized: initialized, defaults to None
    :type initialized: int, optional
    """

    def __init__(
        self, created: int, updated: int, initialized: int = SENTINEL, **kwargs
    ):
        """ProjectTime

        :param created: created
        :type created: int
        :param updated: updated
        :type updated: int
        :param initialized: initialized, defaults to None
        :type initialized: int, optional
        """
        self.created = self._define_number("created", created, ge=0)
        self.updated = self._define_number("updated", updated, ge=0)
        if initialized is not SENTINEL:
            self.initialized = self._define_number("initialized", initialized, ge=0)
        self._kwargs = kwargs


@JsonMap({"id_": "id"})
class Project(BaseModel):
    """Project

    :param id_: id_
    :type id_: str
    :param worktree: worktree
    :type worktree: str
    :param vcs: vcs, defaults to None
    :type vcs: Vcs, optional
    :param name: name, defaults to None
    :type name: str, optional
    :param icon: icon, defaults to None
    :type icon: ProjectIcon, optional
    :param commands: commands, defaults to None
    :type commands: ProjectCommands, optional
    :param time: time
    :type time: ProjectTime
    :param sandboxes: sandboxes
    :type sandboxes: List[str]
    """

    def __init__(
        self,
        id_: str,
        worktree: str,
        time: ProjectTime,
        sandboxes: List[str],
        vcs: Vcs = SENTINEL,
        name: str = SENTINEL,
        icon: ProjectIcon = SENTINEL,
        commands: ProjectCommands = SENTINEL,
        **kwargs
    ):
        """Project

        :param id_: id_
        :type id_: str
        :param worktree: worktree
        :type worktree: str
        :param vcs: vcs, defaults to None
        :type vcs: Vcs, optional
        :param name: name, defaults to None
        :type name: str, optional
        :param icon: icon, defaults to None
        :type icon: ProjectIcon, optional
        :param commands: commands, defaults to None
        :type commands: ProjectCommands, optional
        :param time: time
        :type time: ProjectTime
        :param sandboxes: sandboxes
        :type sandboxes: List[str]
        """
        self.id_ = self._define_str("id_", id_)
        self.worktree = self._define_str("worktree", worktree)
        if vcs is not SENTINEL:
            self.vcs = self._enum_matching(vcs, Vcs.list(), "vcs")
        if name is not SENTINEL:
            self.name = self._define_str("name", name)
        if icon is not SENTINEL:
            self.icon = self._define_object(icon, ProjectIcon)
        if commands is not SENTINEL:
            self.commands = self._define_object(commands, ProjectCommands)
        self.time = self._define_object(time, ProjectTime)
        self.sandboxes = sandboxes
        self._kwargs = kwargs
