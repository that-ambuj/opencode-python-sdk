# This file was generated by liblab | https://liblab.com/

from enum import Enum
from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.sentinel import SENTINEL


class OAuthType(Enum):
    """An enumeration representing different categories.

    :cvar OAUTH: "oauth"
    :vartype OAUTH: str
    """

    OAUTH = "oauth"

    def list():
        """Lists all category values.

        :return: A list of all category values.
        :rtype: list
        """
        return list(map(lambda x: x.value, OAuthType._member_map_.values()))


@JsonMap(
    {"type_": "type", "account_id": "accountId", "enterprise_url": "enterpriseUrl"}
)
class OAuth(BaseModel):
    """OAuth

    :param type_: type_
    :type type_: OAuthType
    :param refresh: refresh
    :type refresh: str
    :param access: access
    :type access: str
    :param expires: expires
    :type expires: int
    :param account_id: account_id, defaults to None
    :type account_id: str, optional
    :param enterprise_url: enterprise_url, defaults to None
    :type enterprise_url: str, optional
    """

    def __init__(
        self,
        type_: OAuthType,
        refresh: str,
        access: str,
        expires: int,
        account_id: str = SENTINEL,
        enterprise_url: str = SENTINEL,
        **kwargs
    ):
        """OAuth

        :param type_: type_
        :type type_: OAuthType
        :param refresh: refresh
        :type refresh: str
        :param access: access
        :type access: str
        :param expires: expires
        :type expires: int
        :param account_id: account_id, defaults to None
        :type account_id: str, optional
        :param enterprise_url: enterprise_url, defaults to None
        :type enterprise_url: str, optional
        """
        self.type_ = self._enum_matching(type_, OAuthType.list(), "type_")
        self.refresh = self._define_str("refresh", refresh)
        self.access = self._define_str("access", access)
        self.expires = self._define_number("expires", expires, ge=0)
        if account_id is not SENTINEL:
            self.account_id = self._define_str("account_id", account_id)
        if enterprise_url is not SENTINEL:
            self.enterprise_url = self._define_str("enterprise_url", enterprise_url)
        self._kwargs = kwargs
