# This file was generated by liblab | https://liblab.com/

from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.sentinel import SENTINEL


@JsonMap({"start_command": "startCommand"})
class WorktreeCreateInput(BaseModel):
    """WorktreeCreateInput

    :param name: name, defaults to None
    :type name: str, optional
    :param start_command: Additional startup script to run after the project's start command, defaults to None
    :type start_command: str, optional
    """

    def __init__(self, name: str = SENTINEL, start_command: str = SENTINEL, **kwargs):
        """WorktreeCreateInput

        :param name: name, defaults to None
        :type name: str, optional
        :param start_command: Additional startup script to run after the project's start command, defaults to None
        :type start_command: str, optional
        """
        if name is not SENTINEL:
            self.name = self._define_str("name", name)
        if start_command is not SENTINEL:
            self.start_command = self._define_str("start_command", start_command)
        self._kwargs = kwargs
