# This file was generated by liblab | https://liblab.com/

from __future__ import annotations
from enum import Enum
from typing import List
from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.sentinel import SENTINEL
from .file_part_source import FilePartSource, FilePartSourceGuard
from .file_source import FileSource
from .symbol_source import SymbolSource
from .resource_source import ResourceSource


class PartsType(Enum):
    """An enumeration representing different categories.

    :cvar FILE: "file"
    :vartype FILE: str
    """

    FILE = "file"

    def list():
        """Lists all category values.

        :return: A list of all category values.
        :rtype: list
        """
        return list(map(lambda x: x.value, PartsType._member_map_.values()))


@JsonMap({"id_": "id", "type_": "type"})
class SessionCommandRequestParts(BaseModel):
    """SessionCommandRequestParts

    :param id_: id_, defaults to None
    :type id_: str, optional
    :param type_: type_
    :type type_: PartsType
    :param mime: mime
    :type mime: str
    :param filename: filename, defaults to None
    :type filename: str, optional
    :param url: url
    :type url: str
    :param source: source, defaults to None
    :type source: FilePartSource, optional
    """

    def __init__(
        self,
        type_: PartsType,
        mime: str,
        url: str,
        id_: str = SENTINEL,
        filename: str = SENTINEL,
        source: FilePartSource = SENTINEL,
        **kwargs,
    ):
        """SessionCommandRequestParts

        :param id_: id_, defaults to None
        :type id_: str, optional
        :param type_: type_
        :type type_: PartsType
        :param mime: mime
        :type mime: str
        :param filename: filename, defaults to None
        :type filename: str, optional
        :param url: url
        :type url: str
        :param source: source, defaults to None
        :type source: FilePartSource, optional
        """
        if id_ is not SENTINEL:
            self.id_ = self._define_str("id_", id_, pattern="^prt")
        self.type_ = self._enum_matching(type_, PartsType.list(), "type_")
        self.mime = self._define_str("mime", mime)
        if filename is not SENTINEL:
            self.filename = self._define_str("filename", filename)
        self.url = self._define_str("url", url)
        if source is not SENTINEL:
            self.source = FilePartSourceGuard.return_one_of(source)
        self._kwargs = kwargs


@JsonMap({"message_id": "messageID"})
class SessionCommandRequest(BaseModel):
    """SessionCommandRequest

    :param message_id: message_id, defaults to None
    :type message_id: str, optional
    :param agent: agent, defaults to None
    :type agent: str, optional
    :param model: model, defaults to None
    :type model: str, optional
    :param arguments: arguments
    :type arguments: str
    :param command: command
    :type command: str
    :param variant: variant, defaults to None
    :type variant: str, optional
    :param parts: parts, defaults to None
    :type parts: List[SessionCommandRequestParts], optional
    """

    def __init__(
        self,
        arguments: str,
        command: str,
        message_id: str = SENTINEL,
        agent: str = SENTINEL,
        model: str = SENTINEL,
        variant: str = SENTINEL,
        parts: List[SessionCommandRequestParts] = SENTINEL,
        **kwargs,
    ):
        """SessionCommandRequest

        :param message_id: message_id, defaults to None
        :type message_id: str, optional
        :param agent: agent, defaults to None
        :type agent: str, optional
        :param model: model, defaults to None
        :type model: str, optional
        :param arguments: arguments
        :type arguments: str
        :param command: command
        :type command: str
        :param variant: variant, defaults to None
        :type variant: str, optional
        :param parts: parts, defaults to None
        :type parts: List[SessionCommandRequestParts], optional
        """
        if message_id is not SENTINEL:
            self.message_id = self._define_str("message_id", message_id, pattern="^msg")
        if agent is not SENTINEL:
            self.agent = self._define_str("agent", agent)
        if model is not SENTINEL:
            self.model = self._define_str("model", model)
        self.arguments = self._define_str("arguments", arguments)
        self.command = self._define_str("command", command)
        if variant is not SENTINEL:
            self.variant = self._define_str("variant", variant)
        if parts is not SENTINEL:
            self.parts = self._define_list(parts, SessionCommandRequestParts)
        self._kwargs = kwargs
