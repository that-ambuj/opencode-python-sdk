# This file was generated by liblab | https://liblab.com/

from __future__ import annotations
from typing import List
from .utils.json_map import JsonMap
from .utils.base_model import BaseModel
from .utils.sentinel import SENTINEL
from .question_option import QuestionOption


@JsonMap({})
class QuestionInfo(BaseModel):
    """QuestionInfo

    :param question: Complete question
    :type question: str
    :param header: Very short label (max 30 chars)
    :type header: str
    :param options: Available choices
    :type options: List[QuestionOption]
    :param multiple: multiple, defaults to None
    :type multiple: bool, optional
    :param custom: custom, defaults to None
    :type custom: bool, optional
    """

    def __init__(
        self,
        question: str,
        header: str,
        options: List[QuestionOption],
        multiple: bool = SENTINEL,
        custom: bool = SENTINEL,
        **kwargs,
    ):
        """QuestionInfo

        :param question: Complete question
        :type question: str
        :param header: Very short label (max 30 chars)
        :type header: str
        :param options: Available choices
        :type options: List[QuestionOption]
        :param multiple: multiple, defaults to None
        :type multiple: bool, optional
        :param custom: custom, defaults to None
        :type custom: bool, optional
        """
        self.question = self._define_str("question", question)
        self.header = self._define_str("header", header)
        self.options = self._define_list(options, QuestionOption)
        if multiple is not SENTINEL:
            self.multiple = multiple
        if custom is not SENTINEL:
            self.custom = custom
        self._kwargs = kwargs
